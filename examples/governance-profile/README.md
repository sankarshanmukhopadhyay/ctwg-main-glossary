# Deprecated governance-profile examples

The earlier governance-profile examples used a reduced profile shape that no longer represents the authoritative term artifact model.

Use `examples/governance-term/` for all new examples. The authoritative source model is the YAML structure under `glossary/terms/`, validated by `schemas/governance-term.schema.json` and `tools/validate_governance_glossary.py`.
