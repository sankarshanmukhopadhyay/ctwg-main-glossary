# sender-constrained-token

A token whose use is cryptographically constrained to a specific sender or key holder, reducing replay risk if the token is copied.

This source definition was added for the v1.2.0 standards-linked glossary refresh. The authoritative operational metadata is maintained in `glossary/terms/sender-constrained-token.yaml`.
